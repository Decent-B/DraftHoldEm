{
  "name": "draft-holdem",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Draft Hold'em — server-authoritative online poker on Cloudflare Durable Objects",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "test:ui": "node scripts/ui-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "wrangler": "^4.45.0",
    "ws": "8.21.3"
  }
}
